# 虚拟图书馆使用与验收说明

## 功能和角色

学生与教师可以检索馆藏、查看本人借阅、借阅、归还和续借。图书管理员可以维护书目及实体馆藏、代办借还、登记破损或遗失并查询所有借阅记录。超级管理员只进入管理页，不能以读者身份借书。所有权限均由服务端再次校验。

## 借阅规则

| 身份 | 同时借阅上限 | 初始借期 | 唯一一次续借 |
| --- | ---: | ---: | ---: |
| 学生 | 5 本 | 30 天 | 15 天 |
| 教师 | 10 本 | 60 天 | 30 天 |

存在任何逾期记录时禁止新增借阅和续借，但仍允许查看与归还。续借从原应还时间延长，服务器回执中的时间是最终依据。

## 读者操作流程

1. 登录后从工作台打开“图书借阅”。
2. 在“图书检索”按书名、作者、ISBN 或分类查询，选择有可借馆藏的书目。
3. 确认借期和预计到期日后提交；成功后以服务器回执为准。
4. 在“我的借阅”切换当前、即将到期、逾期和历史范围。
5. 仅当记录显示“可续借”时执行续借；归还前需再次确认。

## 管理员操作流程

- “书目馆藏”：新建或编辑书目、启停书目、增加实体馆藏、修改馆藏状态。书目和实体馆藏列表均支持分页，最新记录显示在第一页。
- `LOST`、`DAMAGED`、`WITHDRAWN` 必须填写原因；借出中的 `ON_LOAN` 馆藏不能手工改状态。
- “借还办理”：先输入用户名、馆藏条码和业务类型进行校验，确认借阅人、额度、逾期和馆藏状态后才能提交。管理员归还可选择正常、破损或遗失；破损和遗失必须填写原因，并在同一事务中结束借阅及更新馆藏状态。
- “借阅查询”：按用户、姓名、书名或条码搜索，并可筛选活动状态和逾期状态。

## 书目编号、ISBN 与馆藏条码

- 书目编号由服务端自动生成，格式为 `BK` 加 9 位数字；数据库自增主键只用于内部关联，不作为连续业务编号展示。
- ISBN 为选填项。非空时仍必须是校验有效且唯一的 ISBN-10 或 ISBN-13；校内讲义等无 ISBN 资料可以留空。
- 新增实体馆藏时先在书目页选择书目，只需填写书架位置；馆藏条码由服务端自动生成，格式为 `B` 加 9 位数字。
- 两类编号均通过数据库序列表加行锁分配，编号申请和记录创建位于同一事务中，多个客户端并发创建不会获得重复编号。
- 普通读者检索不显示已停用书目。借阅失败会区分“书目已停用”“当前没有可借馆藏”和“刚刚被其他用户借出”。

## 馆藏状态

| 状态 | 含义 |
| --- | --- |
| `AVAILABLE` | 可借阅 |
| `ON_LOAN` | 已借出，由借还事务维护 |
| `LOST` | 遗失并关闭借阅 |
| `DAMAGED` | 损坏，暂停流通 |
| `WITHDRAWN` | 下架，不再流通 |

## 借阅生命周期通知

借阅、续借、正常或破损归还、登记遗失成功后，借阅人分别收到 `LIBRARY_BORROWED`、`LIBRARY_RENEWED`、`LIBRARY_RETURNED` 和 `LIBRARY_LOST`。破损归还使用 `LIBRARY_RETURNED`，标题和正文会明确标注破损及原因。自助操作与管理员代办均发送，内容包含书名、馆藏条码及对应的应还时间、归还时间或异常原因。这些回执与业务状态在同一个数据库事务中提交或回滚。

服务启动时立即扫描一次，之后每小时扫描。应还时间处于未来 3 天内时发送 `LIBRARY_DUE_SOON`；超过应还时间时发送 `LIBRARY_OVERDUE`。通知写入与对应发送标记位于同一数据库事务中，同一笔借阅不会重复发送同类提醒。续借会清除到期提醒标记，以便按照新的应还时间重新提醒。全部六类图书馆消息都可从详情打开“我的借阅”。

## 环境与数据库初始化

- Java 21、MySQL Server 8.0.44、Maven 3.9 或 Eclipse Maven 支持。
- 服务端通过 `VCAMPUS_DB_URL`、`VCAMPUS_DB_USER`、`VCAMPUS_DB_PASSWORD` 获取数据库配置。
- 新数据库依次执行 `database/schema.sql`、`database/seed.sql`。
- 已有数据库应停服备份后，按文件编号依次应用尚未执行的迁移；图书馆至少需要 `003_library.sql`、`004_library_receipt_notifications.sql`、`010_library_usability.sql` 和 `011_library_damaged_returns.sql`，然后再执行 `database/seed.sql`。
- `010_library_usability.sql` 会为既有书目回填业务编号、允许 ISBN 留空，并初始化自动编号序列。书目查询行由 11 个字段升级为 12 个字段，服务端和客户端必须在停服窗口内一起升级，不支持新旧版本混用。
- `011_library_damaged_returns.sql` 扩展借阅归还状态约束，允许管理员直接办理破损归还；迁移应用后必须部署包含 `DAMAGED` 归还枚举的新客户端和服务端。
- 脚本不包含密码。先用超级管理员账号管理创建匿名账号 `2026000001`、`2026000002` 和 `T0000001`，然后重新执行 `seed.sql`，即可生成四类借阅演示记录。

## 课程演示顺序

1. 学生检索并借阅一本可借图书，随后在“我的借阅”查看回执。
2. 学生续借一次，再次续借应被拒绝。
3. 使用逾期学生账号尝试借阅，确认被阻止但仍可归还。
4. 图书管理员新建书目和馆藏，修改损坏状态并填写原因。
5. 管理员通过两阶段校验替学生借书，再正常归还。
6. 管理员将一本借出馆藏按“破损”归还并填写原因，确认借阅关闭且实体馆藏为 `DAMAGED`。
7. 管理员将一本借出馆藏登记为遗失并填写原因。
8. 在借阅查询中验证用户、活动状态和逾期筛选。
9. 等待或手工触发通知扫描，验证到期与逾期消息仅生成一次。
10. 从消息中心打开通知，确认直接进入“我的借阅”。
11. 两个客户端同时借阅最后一本可借馆藏，确认恰好一个成功。

## 并发行为

借阅事务先锁定借阅人及候选馆藏，并使用活动借阅唯一键作为数据库最终防线。两个客户端争抢最后一本馆藏时，只允许一个事务创建活动借阅；另一个客户端收到“该馆藏刚刚被其他用户借出”等可读提示。

## 故障恢复

- 客户端断线或服务端未启动：界面提示连接失败；保留原有表格数据，确认服务端端口后重试。
- 数据库不可用：服务端返回“图书馆数据暂时不可用，请稍后重试”；恢复 MySQL 后无需重启客户端即可再次查询。
- 借阅冲突：刷新馆藏或借阅记录，依据服务器最新状态继续操作。
- 通知扫描失败：本轮事务回滚且不写标记；数据库恢复后下一个小时扫描会再次尝试。
- 迁移约束名称不匹配：先核对数据库是否已应用前序迁移，不要直接删除业务表。

## 验收记录

- 自动化：2026-08-30 执行 `mvn clean verify` 成功；公共模块 56 项、服务端 178 项、客户端 102 项，共 336 项测试，0 失败。
- 演示数据静态检查：6 个唯一且校验有效的 ISBN-13、10 个唯一合法条码、3 个分类。
- MySQL 8.0.44：已备份图书馆核心表，成功应用 `010_library_usability.sql`、`011_library_damaged_returns.sql` 并以 `utf8mb4` 加载 `seed.sql`。回读确认 7 个书目编号连续且唯一（`BK000000001` 至 `BK000000007`）、下一编号为 8、6 本演示书和 10 个演示馆藏齐全；事务内验证确认 `DAMAGED` 归还状态被数据库接受且回滚后原记录不变。尚未执行十一项双客户端人工场景，状态保持“待本机 MySQL 双客户端验收”。
