# 银行 JavaFX 工作台

用户已确认 B1 总览、T1 独立三步转账，去掉个性化问候与转账备注。
用户已要求继续执行正式实现；由主代理串行实施。

首页提供余额卡、隐藏余额、状态、刷新、转账、流水、商店订单、最近流水。
转账先填写账号/金额，再通过只读服务端核对显示名，确认后提交并展示回执。
输入合法金额采用 MoneyPolicy。预检不开户、不改余额，实际交易重新校验状态与余额。
提交超时保留操作编号、目标与金额，不得显示确定失败或换编号重复扣款。

流水保留全部五种类型、分页，增加日期（上海时区自然日）、关键字与业务编号查询。
日期范围包含开始日与结束日。总计由数据库对完整筛选结果聚合。
详情显示已有流水字段；个人商店支付/退款通过受权限约束的关联查询打开订单。
管理员本人流水必须与全校流水明确分开。管理员可检索账户、按用户名核对未开户用户、
充值、冻结/解冻、查指定账户与全量流水；操作均显示目标和最终金额后确认。

保持 Java 21、现有 Socket 协议、三层架构、服务器权限校验、
BigDecimal/DECIMAL、幂等编号、事务内流水和通知。此次无需数据库结构迁移。
JavaFX 替换嵌入式银行入口，兼容 Swing。FX 线程更新界面，后台线程请求，
切页和登出隔离过期结果。通知仅携带账户编号时进入本人流水，不误当成流水 ID。

浏览器草图是视觉参考，正式代码不得使用草图演示数据。
贷款、提现、真实支付、用户自行充值不在范围内。
